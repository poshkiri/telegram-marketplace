# 🚂 Деплой Telegram Marketplace на Railway.app

## 📋 Подготовка к деплою

### ✅ Что нужно перед деплоем:

1. **GitHub репозиторий** с вашим кодом
2. **MongoDB Atlas** (бесплатная облачная база данных)
3. **Telegram Bot Token** от @BotFather
4. **USDT кошельки** (минимум TRC20)

---

## 🗄️ Шаг 1: Настройка MongoDB Atlas (БЕСПЛАТНО)

### 1.1 Создайте аккаунт

1. Перейдите на https://www.mongodb.com/cloud/atlas
2. Нажмите **"Try Free"** или **"Sign Up"**
3. Зарегистрируйтесь (можно через Google)

### 1.2 Создайте кластер

1. После регистрации выберите **"Build a Database"**
2. Выберите **FREE** тариф (M0 Sandbox)
3. Выберите провайдера (AWS, Google Cloud, Azure)
4. Выберите регион (ближайший к вам)
5. Нажмите **"Create"**

### 1.3 Настройте доступ

1. **Создайте пользователя базы данных:**
   - Username: `marketplace_user` (или любое имя)
   - Password: создайте надежный пароль (сохраните!)
   - Нажмите **"Create User"**

2. **Настройте Network Access:**
   - Нажмите **"Network Access"** в меню слева
   - Нажмите **"Add IP Address"**
   - Выберите **"Allow Access from Anywhere"** (0.0.0.0/0)
   - Нажмите **"Confirm"**

### 1.4 Получите строку подключения

1. Нажмите **"Database"** в меню слева
2. Нажмите **"Connect"** на вашем кластере
3. Выберите **"Connect your application"**
4. Выберите **"Node.js"** и версию **"4.1 or later"**
5. Скопируйте строку подключения:

```
mongodb+srv://marketplace_user:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
```

6. **Замените `<password>`** на ваш пароль
7. **Добавьте имя базы данных** в конце:

```
mongodb+srv://marketplace_user:ваш_пароль@cluster0.xxxxx.mongodb.net/marketplace?retryWrites=true&w=majority
```

**Сохраните эту строку!** Она понадобится для Railway.

---

## 🚀 Шаг 2: Подготовка кода для деплоя

### 2.1 Убедитесь, что код готов

Проверьте, что в `telegram-bot/bot.js` используется:

```javascript
const token = process.env.TELEGRAM_BOT_TOKEN;
```

✅ Это уже правильно в вашем коде!

### 2.2 Проверьте package.json

Убедитесь, что есть скрипт:

```json
{
  "scripts": {
    "start": "node telegram-bot/bot.js"
  }
}
```

✅ Это уже есть!

### 2.3 Убедитесь, что .env в .gitignore

✅ Уже добавлено в `.gitignore`!

---

## 🚂 Шаг 3: Деплой на Railway

### 3.1 Загрузите код на GitHub

Если код еще не на GitHub:

```bash
cd telegram-marketplace
git init
git add .
git commit -m "Ready for Railway deploy"
```

Создайте репозиторий на GitHub и загрузите:

```bash
git remote add origin https://github.com/ваш-username/telegram-marketplace.git
git branch -M main
git push -u origin main
```

### 3.2 Зарегистрируйтесь на Railway

1. Перейдите на https://railway.app
2. Нажмите **"Start a New Project"**
3. Войдите через **GitHub**
4. Разрешите доступ Railway к вашему GitHub

### 3.3 Создайте проект

1. В Railway нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Найдите и выберите ваш репозиторий `telegram-marketplace`
4. Railway автоматически определит Node.js проект

### 3.4 Настройте переменные окружения

В Railway откройте ваш проект → **"Variables"** и добавьте:

#### Обязательные переменные:

```
TELEGRAM_BOT_TOKEN = ваш_токен_от_BotFather
MONGODB_URI = mongodb+srv://marketplace_user:пароль@cluster0.xxxxx.mongodb.net/marketplace?retryWrites=true&w=majority
```

#### Важные переменные (минимум для работы):

```
WALLET_TRC20 = ваш_tron_кошелек_адрес
ADMIN_TELEGRAM_ID = ваш_telegram_id
```

#### Опциональные переменные:

```
WALLET_ERC20 = ваш_ethereum_кошелек (если нужен)
WALLET_BEP20 = ваш_bsc_кошелек (если нужен)
TRONGRID_API_KEY = ваш_api_ключ (опционально)
PORT = 3000 (Railway автоматически установит)
NODE_ENV = production
COMMISSION_RATE = 0.05
ESCROW_HOURS = 24
MIN_PRICE = 1
MAX_PRICE = 10000
JWT_SECRET = случайная_строка_для_безопасности
SESSION_SECRET = другая_случайная_строка
```

### 3.5 Дождитесь деплоя

1. Railway автоматически начнет деплой
2. Откройте вкладку **"Deployments"** для просмотра процесса
3. Дождитесь успешного деплоя (зеленая галочка)

### 3.6 Проверьте логи

1. В Railway откройте **"Deployments"**
2. Нажмите на последний деплой
3. Откройте **"View Logs"**
4. Должны увидеть:
   ```
   ✅ MongoDB подключена
   🤖 Бот запущен!
   ```

---

## ✅ Шаг 4: Проверка работы

### 4.1 Проверьте бота в Telegram

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Бот должен ответить! 🎉

### 4.2 Проверьте health check (опционально)

1. В Railway откройте **"Settings"**
2. Прокрутите до **"Networking"**
3. Нажмите **"Generate Domain"**
4. Откройте URL в браузере
5. Должны увидеть информацию об API

---

## 🔧 Настройка для работы 24/7

Railway автоматически:
- ✅ Держит бота онлайн
- ✅ Перезапускает при ошибках
- ✅ Обновляет при push в GitHub (если включен автодеплой)

### Включить автодеплой:

1. В Railway → **"Settings"**
2. Найдите **"Source"**
3. Убедитесь, что **"Auto Deploy"** включен

---

## 🐛 Решение проблем

### Проблема: "Bot token is empty"

**Решение:**
1. Проверьте Variables в Railway
2. Убедитесь, что переменная называется точно `TELEGRAM_BOT_TOKEN`
3. Перезапустите проект (Settings → Restart)

### Проблема: "MongoDB connection failed"

**Решение:**
1. Проверьте строку подключения MongoDB Atlas
2. Убедитесь, что IP адрес добавлен в Network Access (0.0.0.0/0)
3. Проверьте, что пароль правильный (без символов, которые нужно экранировать)
4. Проверьте логи в Railway

### Проблема: Бот не отвечает

**Решение:**
1. Проверьте логи в Railway (Deployments → View Logs)
2. Убедитесь, что токен правильный
3. Проверьте, что деплой успешен (зеленая галочка)
4. Убедитесь, что MongoDB подключена (должно быть в логах)

### Проблема: "Deployment failed"

**Решение:**
1. Проверьте логи на ошибки
2. Убедитесь, что все зависимости в `package.json`
3. Проверьте, что скрипт `"start"` правильный
4. Убедитесь, что Node.js версия совместима (Railway использует последнюю LTS)

---

## 📊 Мониторинг

### Просмотр логов:

1. Railway → **"Deployments"** → **"View Logs"**
2. Видно все сообщения бота в реальном времени

### Использование ресурсов:

Railway показывает:
- **CPU** - использование процессора
- **Memory** - использование памяти
- **Network** - сетевой трафик

Бесплатный тариф: $5 кредитов в месяц (обычно хватает для бота)

---

## 🔄 Обновление кода

После изменений в коде:

1. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "Update bot"
   git push
   ```

2. Railway автоматически задеплоит новую версию (если включен Auto Deploy)

3. Или вручную: Railway → **"Deployments"** → **"Redeploy"**

---

## 💡 Полезные советы

1. **Всегда проверяйте логи** - там видно все ошибки
2. **Не коммитьте `.env`** - используйте Variables в Railway
3. **Используйте MongoDB Atlas** - бесплатно и надежно
4. **Тестируйте локально** - перед деплоем убедитесь, что бот работает
5. **Мониторьте использование** - следите за лимитами бесплатного тарифа

---

## 📝 Чек-лист перед деплоем

- [ ] Код загружен на GitHub
- [ ] MongoDB Atlas настроен и работает
- [ ] Получен токен бота от @BotFather
- [ ] Есть USDT кошельки (минимум TRC20)
- [ ] Все переменные окружения подготовлены
- [ ] `.env` файл НЕ в Git (проверено через `.gitignore`)
- [ ] `package.json` имеет скрипт `"start"`
- [ ] Код использует `process.env` для всех секретов

---

## 🎉 Готово!

Ваш Telegram Marketplace теперь работает 24/7 на Railway!

**Что дальше:**
- ✅ Бот автоматически отвечает на команды
- ✅ Данные сохраняются в MongoDB Atlas
- ✅ Бот работает без вашего компьютера
- ✅ Автоматические обновления при push в GitHub

---

## 📚 Дополнительные ресурсы

- [Railway Документация](https://docs.railway.app/)
- [MongoDB Atlas Документация](https://docs.atlas.mongodb.com/)
- [Telegram Bot API](https://core.telegram.org/bots/api)

---

**Удачи с деплоем! 🚀**

