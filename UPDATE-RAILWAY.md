# 🚀 Быстрое обновление на Railway

## Шаги для обновления бота на Railway:

### 1. Откройте Git Bash (mingw64)

### 2. Перейдите в папку проекта:
```bash
cd /c/Users/Светлана/Desktop/cursor/telegram-marketplace
```

### 3. Проверьте изменения:
```bash
git status
```

### 4. Добавьте все изменения:
```bash
git add .
```

### 5. Создайте коммит:
```bash
git commit -m "Fix: исправлена локализация для английского языка"
```

### 6. Отправьте на GitHub:
```bash
git push origin main
```

(Если ветка называется `master` вместо `main`, используйте `git push origin master`)

### 7. Railway автоматически обновит бота!

Railway автоматически:
- ✅ Обнаружит новый коммит
- ✅ Запустит новый деплой
- ✅ Перезапустит бота с обновленным кодом

### 8. Проверьте деплой:

1. Откройте Railway: https://railway.app
2. Выберите ваш проект
3. Перейдите в **"Deployments"**
4. Дождитесь завершения деплоя (зеленая галочка)
5. Проверьте логи: **"View Logs"**

Должны увидеть:
```
✅ MongoDB подключена
🤖 Бот запущен и готов к работе!
```

### 9. Протестируйте бота:

1. Откройте Telegram
2. Найдите вашего бота
3. Выберите английский язык
4. Проверьте, что все функции работают на английском

---

## ⚠️ Если Railway не обновляется автоматически:

### Вариант 1: Перезапуск вручную
1. В Railway → **"Settings"**
2. Нажмите **"Restart"**

### Вариант 2: Проверьте автодеплой
1. В Railway → **"Settings"**
2. Найдите **"Source"**
3. Убедитесь, что **"Auto Deploy"** включен

---

## 🐛 Если что-то пошло не так:

### Проблема: "git push" не работает
```bash
# Проверьте, подключен ли удаленный репозиторий:
git remote -v

# Если нет, добавьте:
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
```

### Проблема: Бот не запускается после обновления
1. Проверьте логи в Railway
2. Убедитесь, что все переменные окружения на месте:
   - `TELEGRAM_BOT_TOKEN`
   - `MONGODB_URI`
3. Перезапустите проект вручную

---

## ✅ Готово!

После успешного деплоя ваш бот будет работать с исправленной локализацией!

