# 📤 Как загрузить код на GitHub через командную строку

## 🖥️ Шаг 1: Откройте командную строку

### Windows:

**Вариант 1: Через проводник (самый простой)**
1. Откройте папку `telegram-marketplace` в проводнике
2. В адресной строке введите `cmd` и нажмите Enter
3. Откроется командная строка в этой папке!

**Вариант 2: Через PowerShell**
1. Нажмите `Win + R`
2. Введите `powershell` и нажмите Enter
3. Перейдите в папку проекта:
   ```powershell
   cd C:\Users\Светлана\Desktop\cursor\telegram-marketplace
   ```

**Вариант 3: Через меню Пуск**
1. Нажмите `Win + X`
2. Выберите "Windows PowerShell" или "Терминал"
3. Перейдите в папку:
   ```powershell
   cd C:\Users\Светлана\Desktop\cursor\telegram-marketplace
   ```

---

## 📦 Шаг 2: Проверьте, установлен ли Git

В командной строке введите:

```bash
git --version
```

**Если видите версию** (например: `git version 2.40.0`) - ✅ Git установлен!

**Если ошибка** - нужно установить Git:
1. Скачайте с https://git-scm.com/download/win
2. Установите (всё по умолчанию)
3. Перезапустите командную строку

---

## 🚀 Шаг 3: Инициализируйте Git (если еще не сделано)

В командной строке выполните:

```bash
git init
```

Должно появиться: `Initialized empty Git repository...`

---

## ✅ Шаг 4: Добавьте файлы

```bash
git add .
```

Эта команда добавит все файлы проекта (кроме тех, что в `.gitignore`)

---

## 💾 Шаг 5: Создайте коммит

```bash
git commit -m "Ready for Railway deploy"
```

**Если видите ошибку** про имя и email:
```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш@email.com"
```

Затем повторите `git commit -m "Ready for Railway deploy"`

---

## 🌐 Шаг 6: Создайте репозиторий на GitHub

1. Откройте https://github.com в браузере
2. Войдите в аккаунт (или зарегистрируйтесь)
3. Нажмите **"+"** в правом верхнем углу
4. Выберите **"New repository"**
5. Заполните:
   - **Repository name:** `telegram-marketplace` (или любое имя)
   - **Description:** (опционально) "Telegram Marketplace Bot"
   - **Public/Private:** выберите (Public - бесплатно)
   - ❌ **НЕ ставьте галочки** на "Add a README file", "Add .gitignore", "Choose a license"
6. Нажмите **"Create repository"**

---

## 📤 Шаг 7: Подключите GitHub и загрузите код

GitHub покажет инструкции. Выполните команды:

```bash
git remote add origin https://github.com/ваш-username/telegram-marketplace.git
```

**Замените `ваш-username`** на ваш GitHub username!

Например, если ваш username `svetlana`, то:
```bash
git remote add origin https://github.com/svetlana/telegram-marketplace.git
```

Затем:

```bash
git branch -M main
git push -u origin main
```

**При первом push** GitHub попросит авторизацию:
- Может попросить логин и пароль
- Или откроется браузер для авторизации

---

## ✅ Готово!

После успешной загрузки вы увидите:
```
Enumerating objects: ...
Writing objects: ...
To https://github.com/ваш-username/telegram-marketplace.git
 * [new branch]      main -> main
```

Теперь ваш код на GitHub! 🎉

---

## 🐛 Решение проблем

### Проблема: "git: command not found"
**Решение:** Установите Git с https://git-scm.com/download/win

### Проблема: "Please tell me who you are"
**Решение:** Выполните:
```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш@email.com"
```

### Проблема: "Permission denied" при push
**Решение:** 
- Проверьте правильность URL репозитория
- Убедитесь, что вы авторизованы в GitHub
- Попробуйте использовать GitHub Desktop (проще для новичков)

### Проблема: "fatal: not a git repository"
**Решение:** Вы не в папке проекта. Выполните:
```bash
cd C:\Users\Светлана\Desktop\cursor\telegram-marketplace
```

---

## 💡 Альтернатива: GitHub Desktop (проще!)

Если командная строка кажется сложной, используйте **GitHub Desktop**:

1. Скачайте: https://desktop.github.com/
2. Установите и войдите в GitHub
3. File → Add Local Repository
4. Выберите папку `telegram-marketplace`
5. Нажмите "Publish repository"
6. Готово! 🎉

---

## 📝 Быстрая шпаргалка команд:

```bash
# 1. Открыть папку в командной строке
cd C:\Users\Светлана\Desktop\cursor\telegram-marketplace

# 2. Инициализировать Git
git init

# 3. Добавить файлы
git add .

# 4. Создать коммит
git commit -m "Ready for deploy"

# 5. Подключить GitHub (замените username!)
git remote add origin https://github.com/ваш-username/telegram-marketplace.git

# 6. Загрузить на GitHub
git branch -M main
git push -u origin main
```

---

**После загрузки на GitHub → переходите к деплою на Railway!** 🚂

