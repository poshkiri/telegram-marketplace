# 🚀 Что делать после настройки .env

## ✅ У вас готовый .env файл? Отлично! Теперь деплоим на Railway!

---

## 📋 Шаг 1: Проверьте .env файл

Убедитесь, что в вашем `.env` файле заполнены **минимум** эти переменные:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/marketplace?retryWrites=true&w=majority
WALLET_TRC20=ваш_tron_кошелек
ADMIN_TELEGRAM_ID=ваш_telegram_id
```

**Важно:** 
- ❌ **НЕ коммитьте** `.env` файл в Git!
- ✅ Убедитесь, что `.env` в `.gitignore`

---

## 📦 Шаг 2: Загрузите код на GitHub

### Если код еще НЕ на GitHub:

1. Откройте командную строку в папке `telegram-marketplace`
2. Выполните:

```bash
git init
git add .
git commit -m "Ready for Railway deploy"
```

3. Создайте репозиторий на GitHub:
   - Зайдите на https://github.com
   - Нажмите **"+"** → **"New repository"**
   - Назовите (например: `telegram-marketplace`)
   - **НЕ** ставьте галочки (README, .gitignore)
   - Нажмите **"Create repository"**

4. Загрузите код:

```bash
git remote add origin https://github.com/ваш-username/telegram-marketplace.git
git branch -M main
git push -u origin main
```

### Если код УЖЕ на GitHub:

Просто убедитесь, что последние изменения загружены:

```bash
git add .
git commit -m "Update for deploy"
git push
```

---

## 🚂 Шаг 3: Зарегистрируйтесь на Railway

1. Перейдите на **https://railway.app**
2. Нажмите **"Start a New Project"**
3. Войдите через **GitHub** (разрешите доступ)

---

## 🚀 Шаг 4: Создайте проект на Railway

1. В Railway нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Найдите и выберите ваш репозиторий `telegram-marketplace`
4. Railway автоматически определит Node.js проект и начнет деплой

---

## ⚙️ Шаг 5: Добавьте переменные окружения

**Это самый важный шаг!**

1. В Railway откройте ваш проект
2. Нажмите на сервис (обычно называется как ваш репозиторий)
3. Перейдите во вкладку **"Variables"**
4. Нажмите **"+ New Variable"**

### Добавьте ВСЕ переменные из вашего .env файла:

**Обязательные:**
```
TELEGRAM_BOT_TOKEN = ваш_токен_из_env
MONGODB_URI = ваша_строка_подключения_из_env
WALLET_TRC20 = ваш_кошелек_из_env
ADMIN_TELEGRAM_ID = ваш_id_из_env
```

**Важные (если есть в .env):**
```
WALLET_ERC20 = ваш_ethereum_кошелек (если есть)
WALLET_BEP20 = ваш_bsc_кошелек (если есть)
```

**Опциональные (если есть в .env):**
```
TRONGRID_API_KEY = ваш_ключ (если есть)
ETHERSCAN_API_KEY = ваш_ключ (если есть)
BSCSCAN_API_KEY = ваш_ключ (если есть)
COMMISSION_RATE = 0.05 (если есть)
ESCROW_HOURS = 24 (если есть)
MIN_PRICE = 1 (если есть)
MAX_PRICE = 10000 (если есть)
JWT_SECRET = ваш_секрет (если есть)
SESSION_SECRET = ваш_секрет (если есть)
PORT = 3000 (Railway установит автоматически)
NODE_ENV = production
```

**Как добавить:**
- **Key:** название переменной (например: `TELEGRAM_BOT_TOKEN`)
- **Value:** значение из вашего `.env` файла
- Нажмите **"Add"**

**Повторите для каждой переменной!**

---

## ⏳ Шаг 6: Дождитесь деплоя

1. После добавления переменных Railway автоматически перезапустит проект
2. Откройте вкладку **"Deployments"**
3. Дождитесь успешного деплоя (зеленая галочка ✅)
4. Это может занять 2-3 минуты

---

## ✅ Шаг 7: Проверьте работу

### Проверьте логи:

1. В Railway → **"Deployments"**
2. Нажмите на последний деплой
3. Откройте **"View Logs"**
4. Должны увидеть:
   ```
   ✅ MongoDB подключена
   🤖 Бот запущен и готов к работе!
   ```

### Проверьте бота:

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Бот должен ответить! 🎉

---

## 🐛 Если что-то не работает:

### Проблема: "Bot token is empty"
- Проверьте Variables в Railway
- Убедитесь, что переменная называется точно `TELEGRAM_BOT_TOKEN`
- Перезапустите проект (Settings → Restart)

### Проблема: "MongoDB connection failed"
- Проверьте строку подключения `MONGODB_URI`
- Убедитесь, что IP добавлен в MongoDB Atlas (0.0.0.0/0)
- Проверьте пароль в строке подключения

### Проблема: Бот не отвечает
- Проверьте логи в Railway
- Убедитесь, что токен правильный
- Проверьте, что деплой успешен

---

## 🎉 Готово!

Ваш бот теперь работает 24/7 на Railway!

**Что дальше:**
- ✅ Бот автоматически отвечает на команды
- ✅ Данные сохраняются в MongoDB Atlas
- ✅ Бот работает без вашего компьютера
- ✅ Автоматические обновления при push в GitHub

---

## 📝 Быстрая шпаргалка:

1. ✅ `.env` готов → 
2. ✅ Код на GitHub → 
3. ✅ Регистрация на Railway → 
4. ✅ Подключить репозиторий → 
5. ✅ Добавить Variables из .env → 
6. ✅ Дождаться деплоя → 
7. ✅ Проверить бота в Telegram

---

**Полная инструкция:** `RAILWAY-DEPLOY.md`

